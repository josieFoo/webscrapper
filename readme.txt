# Maybe you need to do first:
# pip install -r requirements.txt
# This is a prototype for making a table of items of 'www.buchsys.de'.
# Every item has a registered title on the web page and a link adress.
# Run by main.py
# You will get '.csv' file.